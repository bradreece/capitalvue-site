# Build output. Regenerated by `node build.js` on every build.
# Tracked deliberately (see HANDOVER.md, "Why built output is committed"),
# so nothing here is ignored. This file exists for the genuine junk only.

node_modules/
.netlify/
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# Never commit. These leaked onto the live site once already.
_private/
*.env
.env*
!.env.example

# Local scratch
tmp/
scratch/
*.zip
